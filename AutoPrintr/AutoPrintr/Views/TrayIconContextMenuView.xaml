<UserControl x:Class="AutoPrintr.Views.TrayIconContextMenuView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:AutoPrintr.Converters">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <UserControl.ContextMenu>
        <ContextMenu Placement="MousePoint">
            <MenuItem Header="Login" Command="{Binding GoToLoginCommand}" Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=false}" />
            <MenuItem Header="Jobs" Command="{Binding GoToJobsCommand}" Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=true}" />
            <MenuItem Header="Settings" Command="{Binding GoToSettingsCommand}" Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=true}" />
            <MenuItem Header="Logout" Command="{Binding LogoutCommand}" Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=true}" />
            <MenuItem Header="Exit" Click="ExitMenuItem_Click" />
        </ContextMenu>
    </UserControl.ContextMenu>
</UserControl>